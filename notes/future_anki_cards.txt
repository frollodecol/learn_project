Как переместиться в другую папку из текущей?
    cd
Как показать содержимое текущей папки?
    dir или ls
Как создать папку через консоль?
    md folderName
Как показать скрытое содержимое директории через консоль виндовса?
    ls -force // ls -a почему-то не работает
Как задать имя пользователя гит?
    git config --global user.name username (не забывай, что config используется без двух дефисов)
Как задать email пользователя в гит?
    git config --global user.email useremail@mail.com
Как посмтреть конфиги гитхаба?
    git config --list
Как "добавить" все файлы для коммиченья в репозитории?
    git add .
Как сделать коммит?
    git commit -m "first commit"
Как залить текущую папку в репозиторий гитхаба?
    git remote add origin git@github.com:frollodecol/learn_project.git (обрати внимание, хоть .git и является "дочерней папкой" репозитория) 
    этой строкой всего лишь произошла инициализация. После этого надо вбить 
    git push -u origin master
Как создать ключ ssh?
    Нужно две команды. Первая - чтоб перейти в нужную папку, вторая - чтоб сгенерировать ключ.
    cd ~/.ssh
    ssh-keygen
    Далее просто жми энтеры. Ключ, который надо отправить на сайт, находится в файле с расширением pub, и он достаточно объёмный. Это не отпечаток, что высвечивается в консоли.
    Будет создано два файла. Приватный ключ (его никому не давай), и публичный (с расширением .pub)
Как клонировать удалённый репозиторий на локальную машину?
    git clone git@github.com:frollodecol/learn_project.git
Как сделать перенос строк в VSCode?
    alt + z
Какой флаг использовать, чтоб добавить пакет в devDependencies?
    npm install --save-dev
Как запустить скрипт через npm?
    Сначала пропиши его в package.json, потом запусти с помощью npm scriptname
Какую команду нужно вбить, чтоб начать использовать npm в проекте?
    npm init
Чем package.json отличается от package-lock.json?
    В package-lock json указывается точный номер версии пакетов.
Как создать файл через cmd в windows?
    copy con filename
    Затем нужно прописать содержимое файла, его, если что, можно оставить пустым.
    Далее прожимай ctrl + c или f6
Как называется интерпретатор для хрома и ноды?
    v8.
Что такое интерпретатор?
    Хуйня, которая код языка программирования переводит в машинный код.
Как расшифровывается LTS?
    Long term support.
Как расшифровывается CLI?
    Command line interface. Например, babel/cli - консольная утилита, позволяющая запускать babel из консоли.
Как сделать "билд" в babel?
    В package.json прописываешь в скриптах "build": "babel src -d lib" // будет перемещено содержимое папки src в папку lib папку lib лучше называть build
Через какие три стадии проходит исходный код, проходящий через babel?
    parsing, transforming, print
Как расшифровавывется CR?
    Carriage return, возврат каретки, перенос курсора на начала строки без перемещения на новую строку.
Как расшифровывается LF?
    Line feed, перенос строки, переходит на строку ниже, при этом, не возвращает курсор на её начало.
Как выйти из vim?
    :q
Как называется аналог ls в cmd?
    dir
Как удалить файл в текущей папке через cmd?
    del filename.ext
Каким хоткеем в VSCode переключаться между терминалом и кодом?
    ctrl + ` (терминал)
    ctrl + 1 (код (первое окно))
Команда для проверки состояния репозитория
    git status
Какую команду использовать для отмены "недобавленных" изменений?
    git checkout . // вместо точки можно поставить имя какого-то файла.
Как выйти из режима вставки в vim?
    esc
Как выйти из вима, сохранив файл?
    :wq
Зачем в git commit -m "Description of the current commit" нужен флажок -m?
    Для того чтоб вбить описание прямо в командной строке, не заходя в текстовый редактор по умолчанию.
Как в vim переключиться в режим вставки?
    i
Как посмотреть список изменений?
    git log
Как отобразить лог изменеий в однострочном режиме?
    git log --pretty=oneline
Как добавить сокращения для гита?
    git config --global alias.hist "log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short" // здесь hist - короткое обозначение, а то, что дальше идёт в кавычках - то, что одним собой заменяет этот самый hist.
Как расшифровывается ASCII?
    American standart code for information interchange
Как вернуться к какой-то из предыдущих версий?
    git checkout <hast_of_necessary_commit> // достаточно нескольких первых символов, хэш вбивается без кавычек и каких бы то ни было скобочек
Как посмотреть содержимое текстового файла в cmd?
    type filename.ext
Как откатиться на одну версию назад?
    git checkout master~1 //вместо master можно использовать тег или хэш
Как задать текущему коммиту тег?
    git tag tagname // например, можно использовать вместо tagname v1
как посмотреть список имеющихся тегов?
    git tag
Как переключать музыку в youtube-music?
    shift + alt + 8
Как в гите сбросить уже "добавленные" изменения?
    git reset version . // version - хэш, тег или иное (head, master) обозначение коммита, . - вместо неё может быть имя файла
Как отменить какие-то закоммиченные изменения?
    git revert HEAD --no-edit// вместо HEAD можно использовать хэш или тег, флаг --no-edit не обязателен, нужен, чтоб не открывался текстовый редактор
Как не просто отменить какие-то закоммиченные изменения, но и убрать их из лога?
    git reset --hard v1 // здесь вместо v1 пиши тег или хэш коммита до которого ты хочешь откатиться
При помощи какого флага можно посмотреть даже сброшенные коммиты?
    all // git hist --all
Как удалить тег?
    git tag -d tagname
Как перезаписать текущий коммит, не плодя новый?
    git commit --amend -m "Description of commit"
Как переместить файл с помощью git, чтоб он сразу был проиндексирован?
    git mv filename.ext foldername // переместить файл в папку
Как в гите удалить файл из списка индексируемых?
    git rm filename.ext
Как в гите создать ветку?
    git checkout -b <branchname> // данная команда является сокращением для двух нижеупомянутых
    git branch <branchname>
    git checkout <branchname>
Что будет если ты запустишь гит из одной из дочерних директорий проекта
    Всё будет худо-бедно нормально.
При помощи какой команды можно переключаться между ветками
    git checkout <branchname>
Какой флаг использовать, чтоб показать все ветки?
    git hist --all // нужно использовать флаг --all
Как добавить к текущей ветке какую-то другую?
    git merge <otherBranchName> // добавит к коду текущей ветки код из другой. Пока они не конфликтуют.
Второй способ добавить к текущей ветке другую
    git rebase <otherBranchName>
Как клонировать репозиторий?
    git clone hello cloned_hello // запускай эту команду, поднявшись в родительскую папку hello.
Как посмотреть список веток в репозитории?
    git branch
Как получить в клонированный репозиторий коммиты (только коммиты!) из оригинального?
    git fetch
Как добавить изменения в клонируемый репозиторий из клонированного, родительского?
    После git fetch вбей 
    git merge origin/master
Какая команда объединяет команды git fetch и git merge origin/master?
    git pull
Как отслеживать удалённую ветку (ветку оригинального репозитория, который ты клонировал себе)
    git branch --track style origin/style // style - ветка в локальном репозитории, origin/style - ветка удалённого, которую ты сейчас "слушаешь"
Как создать чистый репозиторий?
    git clone --bare hello hello.git
Как добавить удалённый репозиторий?
    git remote add shared ..hello.git
Как отправить изменения в общий репозиторий?
    git push shared master
Как выглядит однострочный комментарий в js?
    // comment text
Как выглядит многострочный комментарий в js?
    /* comment text 
    comment text*/
Как в js выглядит объявление переменной?
    var varName;
Как экранировать символы (например, кавычки)
    \"
Перенос строки в js
    \n 
Табуляция в js
    \t
Метод, чтоб добавить что-то в конец массива
    .push()
Метод для удаления элемента из конца массива и его возврата куда-то
    .pop()
Метод для удаления первого элемента массива, возврата этого элемента, и смещения всего массива "влево"
    .shift()
Метод для добавления первого элемента массива и смещения остального массива вправо.
    .unshift
Самый попсовый синтаксис функции в js
    function functionName(a, b) {
        operators;
        return outputVariable
    }
Как определить тип переменной в js?
    typeof varName
    typeof(varName)
Как разделить напополам окно с кодом в VSCode?
    ctrl + 2
